package com.mishadoff.algo.arrays;

/**
 * Interface for maximum subarray problem.
 * Find contiguous subarray from array which have maximum sum of elements.
 *
 * @author mishadoff
 */
public interface MaximumSubarray {
    int[] find(int[] array);
}
